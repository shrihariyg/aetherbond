global:
  scrape_interval:     5s # High-fidelity monitoring for rapid routing decisions
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'aetherbond'
    static_configs:
      - targets: ['127.0.0.1:9100']
        labels:
          group: 'multipath_clients'
